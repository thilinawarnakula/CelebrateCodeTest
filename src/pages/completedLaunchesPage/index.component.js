import React from 'react';
import {
    SafeAreaView,
} from 'react-native';
import styles from './index.styles';

const CompletedLaunchesPage = ({ navigation }) => {

    return (
        <SafeAreaView style={styles.mainContainer}>
            
        </SafeAreaView>
    )
}

export default CompletedLaunchesPage;